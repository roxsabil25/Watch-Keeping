const categories = ['All', 'Chronograph', 'Automatic', 'Leather Strap', 'Metal Bracelet', 'Pre-Order']
export default function FilterBar({ selected, setSelected }) { return <section className="flex flex-wrap items-center justify-between gap-4 bg-[#FAFAF8] px-6 py-6"><div className="flex flex-wrap gap-3">{categories.map((category) => <button key={category} onClick={() => setSelected(category)} className={`rounded-full border px-5 py-2 text-[10px] uppercase tracking-widest transition ${selected === category ? 'border-black bg-black text-white' : 'border-black/10 bg-white hover:border-black'}`}>{category}</button>)}</div><select className="rounded-full border border-black/10 bg-white px-4 py-2 text-xs outline-none" aria-label="Sort products"><option>Sort: Featured</option><option>Sort: Low to High</option><option>Sort: High to Low</option></select></section> }