import { motion, useScroll, useTransform } from 'framer-motion'

export default function ProductCard({ product, onQuickView, index = 0 }) {
	const { scrollYProgress } = useScroll()
	const imageY = useTransform(scrollYProgress, [0, 1], [0, -50])
	return <motion.article onClick={() => onQuickView(product)} data-aos="fade-up" data-aos-delay={index * 120} data-aos-duration="1000" data-aos-anchor-placement="top-bottom" className="group cursor-pointer overflow-hidden rounded-2xl border border-black/5 bg-white transition duration-700 hover:-translate-y-2 hover:shadow-[0_25px_50px_rgba(0,0,0,0.1)]"><div className="relative h-72 overflow-hidden rounded-2xl bg-[#FAFAF8] p-6"><motion.img style={{ y: imageY }} data-aos="zoom-in" data-aos-delay="200" data-aos-duration="1100" src={product.image} alt={product.model} className="h-full w-full object-contain transition-transform duration-700 ease-[cubic-bezier(0.16,1,0.3,1)] will-change-transform group-hover:scale-110"/><span className="absolute left-3 top-3 rounded-full bg-white/90 px-3 py-1 text-[9px] uppercase tracking-widest backdrop-blur">{product.tag}</span><button onClick={(e) => { e.stopPropagation(); onQuickView(product) }} className="luxury-button absolute bottom-3 left-3 right-3 rounded-full bg-black py-2 text-xs text-white opacity-0 transition group-hover:opacity-100">Quick View</button></div><div className="p-5"><motion.p initial={{ opacity: 0 }} whileInView={{ opacity: 1 }} viewport={{ once: true }} transition={{ delay: index * 0.08 }} className="text-[10px] uppercase tracking-[0.2em] text-gray-400">{product.brand}</motion.p><h3 className="mt-1 text-sm font-medium">{product.model}</h3><p className="mt-2 font-serif text-lg">৳{product.price.toLocaleString()} <span className="text-[#D4AF37]">●</span></p></div></motion.article>
}